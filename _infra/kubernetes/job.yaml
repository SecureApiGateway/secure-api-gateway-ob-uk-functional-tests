apiVersion: batch/v1
kind: Job
metadata:
  name: functional-test
spec:
  template:
    spec:
      containers:
      - name: functional-test
        image: eu.gcr.io/sbat-gcr-develop/securebanking/functional-tests:latest
      restartPolicy: Never
  backoffLimit: 0